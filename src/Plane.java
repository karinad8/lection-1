public class Plane extends AirlineOne{

    public Plane(double capacity, double loadCapacity, String color, String name, double releaseYear, double flightRange) {
        super(capacity, loadCapacity, color, name, releaseYear, flightRange);
    }
}
